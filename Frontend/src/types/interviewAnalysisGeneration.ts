import type { Application } from './application'; import type { Candidate } from './candidate'; import type { Evaluation } from './evaluation'; import type { Interview } from './interview'; import type { InterviewAnalysis } from './interviewAnalysis'; import type { InterviewEvidence } from './interviewEvidence'; import type { InterviewQuestionSet } from './interviewQuestionSet'; import type { InterviewSession } from './interviewSession'; import type { InterviewTranscript } from './interviewTranscript'; import type { Job } from './job'; import type { JobRequirement } from './jobRequirement'
export type InterviewAnalysisGenerationInput = { interview: Interview; session: InterviewSession; transcript: InterviewTranscript; questionSet: InterviewQuestionSet; candidate: Candidate; application: Application; job: Job; requirements: JobRequirement[]; screeningEvaluation?: Evaluation }
export type InterviewAnalysisGenerationResult = Pick<InterviewAnalysis, 'strengths' | 'concerns' | 'missingEvidence' | 'interviewerSummary'> & { evidence: InterviewEvidence[]; generationSummary: string; warnings: string[] }
