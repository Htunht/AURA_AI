import { useState } from 'react'
import type { DecisionDisagreementReason, FinalEvaluation, HumanFinalDecision as HumanDecision } from '../../types/finalEvaluation'
import type { DemoUser } from '../../types/role'
import { canRecordFinalDecision, doesHumanDecisionDifferFromSystem } from '../../utils/finalDecisionPermissions'
import { Badge } from '../ui/Badge'
import { Button } from '../ui/Button'
import { Card } from '../ui/Card'
import { Dialog } from '../ui/Dialog'

const fieldClass = 'mt-1 w-full rounded-aura-sm border border-harbor/20 bg-white px-3 py-2 text-sm text-depth focus:outline-none focus:ring-2 focus:ring-glacier/40'
export type FinalDecisionDraft = { decision: HumanDecision; decisionReason: string; candidateFacingReasonDraft?: string; disagreementReason?: DecisionDisagreementReason; disagreementExplanation?: string; holdReviewDate?: string }
export function HumanFinalDecision({ evaluation, actor, ready, onRecord }: { evaluation: FinalEvaluation; actor: DemoUser; ready: boolean; onRecord: (draft: FinalDecisionDraft) => void }) {
  const [decision, setDecision] = useState<HumanDecision>('HOLD'); const [reason, setReason] = useState(''); const [candidateReason, setCandidateReason] = useState(''); const [disagreementReason, setDisagreementReason] = useState<DecisionDisagreementReason>('EVIDENCE_INTERPRETATION'); const [explanation, setExplanation] = useState(''); const [reviewDate, setReviewDate] = useState(''); const [confirm, setConfirm] = useState(false)
  if (evaluation.status === 'DECIDED') return <Card className="p-5"><div className="flex items-center justify-between gap-2"><h2 className="m-0 text-base font-semibold text-depth">Final decision recorded</h2><Badge tone="accent">{evaluation.humanDecision?.toLocaleLowerCase()}</Badge></div><dl className="mt-4 grid gap-3 text-sm"><div><dt className="text-xs text-aura-text-muted">Decision maker</dt><dd className="mb-0 mt-1 font-semibold text-depth">{evaluation.decidedBy} · {evaluation.decidedByRole?.replaceAll('_', ' ').toLocaleLowerCase()}</dd></div><div><dt className="text-xs text-aura-text-muted">Internal reason</dt><dd className="mb-0 mt-1 text-aura-text-secondary">{evaluation.humanDecisionReason}</dd></div>{evaluation.candidateFacingReasonDraft ? <div><dt className="text-xs text-aura-text-muted">Candidate-facing reason draft</dt><dd className="mb-0 mt-1 text-aura-text-secondary">{evaluation.candidateFacingReasonDraft}</dd></div> : null}{evaluation.differsFromSystem ? <div><dt className="text-xs text-aura-text-muted">Decision differed from recommendation</dt><dd className="mb-0 mt-1 text-aura-text-secondary">{evaluation.disagreementReason?.replaceAll('_', ' ').toLocaleLowerCase()} · {evaluation.disagreementExplanation}</dd></div> : null}</dl><p className="mb-0 mt-4 text-xs font-semibold text-harbor">Read-only audit record</p></Card>
  if (!canRecordFinalDecision(actor.role)) return <Card className="p-5"><h2 className="m-0 text-base font-semibold text-depth">Final decision restricted</h2><p className="mb-0 mt-2 text-sm leading-6 text-aura-text-secondary">Only an authorized hiring decision maker can record the final candidate outcome. The evaluation remains readable.</p><p className="mb-0 mt-3 text-xs text-aura-text-muted">Current role: {actor.role.replaceAll('_', ' ').toLocaleLowerCase()}</p></Card>
  const differs = doesHumanDecisionDifferFromSystem(evaluation.systemRecommendation, decision); const valid = ready && reason.trim().length >= 20 && (!differs || explanation.trim().length >= 20)
  return <><Card className="p-5"><h2 className="m-0 text-base font-semibold text-depth">Authorized human decision required</h2><p className="mb-0 mt-2 text-sm text-aura-text-secondary">Review the complete evidence trail and record the final candidate outcome.</p><p className="mb-0 mt-3 text-xs font-semibold text-harbor">Decision authority · {actor.name}, Hiring Manager</p><fieldset className="mt-4 grid gap-2"><legend className="text-sm font-semibold text-depth">Human final decision</legend>{([['SELECTED', 'Select candidate'], ['REJECTED', 'Reject candidate'], ['HOLD', 'Place on hold']] as const).map(([value, label]) => <label className="flex items-center gap-2 rounded-aura-sm border border-harbor/15 p-3 text-sm font-medium text-depth" key={value}><input type="radio" name="decision" checked={decision === value} onChange={() => setDecision(value)} />{label}</label>)}</fieldset><label className="mt-4 block text-sm font-semibold text-depth">Internal decision reason<textarea className={`${fieldClass} min-h-28`} maxLength={2000} value={reason} onChange={(event) => setReason(event.target.value)} /></label>{decision === 'REJECTED' ? <label className="mt-4 block text-sm font-semibold text-depth">Candidate-facing reason draft<textarea className={`${fieldClass} min-h-24`} maxLength={2000} value={candidateReason} onChange={(event) => setCandidateReason(event.target.value)} /></label> : null}{decision === 'HOLD' ? <label className="mt-4 block text-sm font-semibold text-depth">Optional review date<input className={fieldClass} type="date" value={reviewDate} onChange={(event) => setReviewDate(event.target.value)} /></label> : null}{differs ? <div className="mt-4 rounded-aura-sm border border-aura-warning/30 bg-aura-warning-soft p-4"><p className="m-0 text-sm font-semibold text-depth">This decision differs from the system recommendation.</p><label className="mt-3 block text-sm font-semibold text-depth">Reason for disagreement<select className={fieldClass} value={disagreementReason} onChange={(event) => setDisagreementReason(event.target.value as DecisionDisagreementReason)}><option value="ADDITIONAL_VERIFIED_INFORMATION">Additional verified information</option><option value="EVIDENCE_INTERPRETATION">Evidence interpretation</option><option value="COMPARATIVE_CANDIDATE_DECISION">Comparative candidate decision</option><option value="ROLE_OR_ORGANIZATIONAL_CHANGE">Role or organizational change</option><option value="LEGAL_OR_POLICY_REVIEW">Legal or policy review</option><option value="OTHER_JOB_RELATED_REASON">Other job-related reason</option></select></label><label className="mt-3 block text-sm font-semibold text-depth">Detailed job-related explanation<textarea className={`${fieldClass} min-h-24`} maxLength={2000} value={explanation} onChange={(event) => setExplanation(event.target.value)} /></label></div> : null}<Button className="mt-5 w-full" disabled={!valid} onClick={() => setConfirm(true)}>Record final decision</Button>{!ready ? <p className="mb-0 mt-2 text-xs text-aura-warning">Resolve decision-readiness issues before recording an outcome.</p> : null}</Card><Dialog open={confirm} title="Record final candidate decision?" onClose={() => setConfirm(false)}><p className="mt-0 text-sm leading-6 text-aura-text-secondary">This records the authorized human decision and locks this evaluation. Candidate communication is handled in the next phase.</p><dl className="grid gap-2 rounded-aura-sm bg-frost p-3 text-sm"><div><dt className="text-xs text-aura-text-muted">System recommendation</dt><dd className="m-0 font-semibold text-depth">{evaluation.systemRecommendation.replaceAll('_', ' ').toLocaleLowerCase()}</dd></div><div><dt className="text-xs text-aura-text-muted">Human decision</dt><dd className="m-0 font-semibold text-depth">{decision.toLocaleLowerCase()}</dd></div><div><dt className="text-xs text-aura-text-muted">Decision maker</dt><dd className="m-0 font-semibold text-depth">{actor.name}</dd></div></dl><div className="mt-6 flex justify-end gap-2"><Button variant="ghost" onClick={() => setConfirm(false)}>Cancel</Button><Button onClick={() => { onRecord({ decision, decisionReason: reason, candidateFacingReasonDraft: candidateReason || undefined, disagreementReason: differs ? disagreementReason : undefined, disagreementExplanation: differs ? explanation : undefined, holdReviewDate: reviewDate || undefined }); setConfirm(false) }}>Record decision</Button></div></Dialog></>
}
