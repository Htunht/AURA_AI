import type { Decision } from '../../types/decision'
import type { Evaluation } from '../../types/evaluation'
import { getScreeningRecommendationLabel } from '../../utils/recommendation'
import { Card } from '../ui/Card'

export function InterviewScreeningContext({ evaluation, decision }: { evaluation?: Evaluation; decision?: Decision }) {
  return <Card className="p-5"><h2 className="m-0 text-base font-semibold text-depth">Screening context</h2>{evaluation ? <><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-xs text-aura-text-muted">Recommendation</dt><dd className="mb-0 mt-1 font-semibold text-depth">{getScreeningRecommendationLabel(evaluation.recommendation)}</dd></div><div><dt className="text-xs text-aura-text-muted">Score and confidence</dt><dd className="mb-0 mt-1 font-semibold text-depth">{evaluation.overallScore}/100 · {evaluation.confidence}%</dd></div><div><dt className="text-xs text-aura-text-muted">Top strength</dt><dd className="mb-0 mt-1 text-depth">{evaluation.strengths[0]?.title ?? 'Not recorded'}</dd></div><div><dt className="text-xs text-aura-text-muted">Key concern</dt><dd className="mb-0 mt-1 text-depth">{evaluation.concerns[0]?.title ?? 'None recorded'}</dd></div><div className="col-span-2"><dt className="text-xs text-aura-text-muted">Recruiter decision</dt><dd className="mb-0 mt-1 font-semibold text-depth">{decision ? getScreeningRecommendationLabel(decision.humanRecommendation) : 'Pending'}</dd></div></dl><p className="mb-0 mt-4 border-t border-harbor/10 pt-3 text-xs leading-5 text-aura-text-muted">Use this context to verify evidence during the interview. The screening result is advisory.</p></> : <p className="mb-0 mt-3 text-sm text-aura-text-secondary">No completed screening evaluation is available. The question plan uses role and application evidence.</p>}</Card>
}
